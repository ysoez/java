package data_structure.tree.heap;

import org.junit.jupiter.api.Test;

class MaxArrayHeapTest {

    @Test
    void dsadsa() {
        var heap = new MaxArrayHeap<Integer>(5);
        heap.insert(10);
        heap.insert(5);
        heap.insert(17);
        heap.insert(4);
        heap.insert(22);
    }

}